name: Update badges

on:
  workflow_dispatch:
  
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:
          GH_TOKEN: ${{ secrets.GH_README_BADGES_TOKEN }}
          CREDLY_USER: alberto.dallagiacoma@live.it # optional, but default will use the same from github
